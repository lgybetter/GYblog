import * as types from '../mutation-types';
import Promsie from 'bluebird';

const state = {

}

const getters = {
  user: state => state.user
}

const actions = {
  
}

const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}

